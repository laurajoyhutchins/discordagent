import { ChannelType, Guild, OverwriteResolvable, PermissionFlagsBits } from 'discord.js';
interface ChannelSetup { categoryId:string; agentChannelId:string; roborevChannelId?:string }
export async function createProjectChannels(guild:Guild,projectName:string,includeRoborev=false):Promise<ChannelSetup>{const botMember=guild.members.me!;const permissionOverwrites:OverwriteResolvable[]=[{id:guild.id,deny:[PermissionFlagsBits.ViewChannel]},{id:botMember.id,allow:[PermissionFlagsBits.ViewChannel,PermissionFlagsBits.SendMessages,PermissionFlagsBits.ManageChannels,PermissionFlagsBits.CreatePublicThreads,PermissionFlagsBits.SendMessagesInThreads]}];const category=await guild.channels.create({name:projectName,type:ChannelType.GuildCategory,permissionOverwrites});const agentChannel=await guild.channels.create({name:'agent',type:ChannelType.GuildText,parent:category.id,topic:`Agent tasks for ${projectName}`});const result:ChannelSetup={categoryId:category.id,agentChannelId:agentChannel.id};if(includeRoborev){const roborevChannel=await guild.channels.create({name:'roborev',type:ChannelType.GuildText,parent:category.id,topic:`Roborev code reviews for ${projectName}`});result.roborevChannelId=roborevChannel.id}return result}
export async function deleteProjectChannels(guild:Guild,categoryId:string,agentChannelId:string,roborevChannelId?:string):Promise<void>{const deleteChannel=async(id:string)=>{try{const ch=await guild.channels.fetch(id);if(ch)await ch.delete()}catch{}};await deleteChannel(agentChannelId);if(roborevChannelId)await deleteChannel(roborevChannelId);await deleteChannel(categoryId)}
